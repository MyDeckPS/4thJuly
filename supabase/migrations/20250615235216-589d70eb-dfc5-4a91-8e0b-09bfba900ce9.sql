
-- Drop the user_messages table as it's no longer needed
DROP TABLE IF EXISTS public.user_messages CASCADE;

-- Note: No triggers or functions specifically for user_messages were found that need cleanup
